--  This sql file will be responsible for reading all products from the database. To read all data from a database we use the following syntax: SELECT * FROM Table. Since we are working with the product table, we'll change Table to product.
SELECT * FROM product;
